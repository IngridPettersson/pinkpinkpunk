import React from "react";

export function ShowDog() {
  // const [image, setShowImage] = useState(null);

  // const [name, setName] = useState('Javad');
  // useEffect(() => {
  //   console.log(name);
  // },[name]);

  // const ShowDogImage = () => {
  //   fetch("https://dog.ceo/api/breeds/image/random")
  //     .then((response) => response.json())
  //     .then((result) => setShowImage(result.message));
  // };

  // console.log(image);
  

  return (
    <div>
      {/* <button onClick={ShowDogImage}>See {props.dogName}!</button>
      {image !== null ? <img src={image} alt="A dog"/> : null} */}
    </div>
  ) 
}
